<h1><%= @product.name %></h1>

<%= image_tag @product.image.url(:medium) %>

<table class="table">
  <tr>
    <th>Price</th>
    <th>Description</th>
  </tr>
  <tr>
    <td><%= @product.price %></td>
    <td><%= @product.description %></td>
  </tr>
</table>
